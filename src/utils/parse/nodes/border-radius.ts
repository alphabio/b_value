/**
 * Border-radius parsing utilities.
 *
 * This module handles:
 * - TRBL (Top-Right-Bottom-Left) length/percentage parsing
 * - Corner value parsing for border-radius shorthand
 * - "round" keyword border-radius parsing (for clip-path shapes)
 *
 * Used by:
 * - clip-path shapes (inset, rect, xywh)
 * - border-radius properties
 *
 * @module utils/parse/nodes/border-radius
 */

// Functions will be moved here in Phase 5
