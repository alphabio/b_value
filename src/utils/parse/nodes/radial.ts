/**
 * Radial shape size parsing utilities.
 *
 * This module handles:
 * - Radial size keywords (closest-side, farthest-side, etc.)
 * - Length/percentage values for radii
 * - Non-negative validation
 * - Special handling for "at" keyword
 *
 * Used by:
 * - clip-path circle() and ellipse()
 * - radial-gradient sizing
 *
 * @module utils/parse/nodes/radial
 */

// Functions will be moved here in Phase 5
